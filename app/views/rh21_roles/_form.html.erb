<%= form_for(rh21_role) do |f| %>
  <% if rh21_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rh21_role.errors.count, "error") %> prohibited this rh21_role from being saved:</h2>

      <ul>
      <% rh21_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %>
    <%= f.text_area :role %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
