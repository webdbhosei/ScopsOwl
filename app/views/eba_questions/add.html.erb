<h1>New Eba Answer</h1>

<%= form_for(@eba_answer) do |f| %>
  <% if @eba_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eba_answer.errors.count, "error") %> prohibited this eba_answer from being saved:</h2>

      <ul>
      <% @eba_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :eba_question_id, value: @eba_question.id %>
  </div>

  <div class="field">
    <%= f.label :answer %>
    <%= f.text_area :answer %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', eba_answers_path %>
