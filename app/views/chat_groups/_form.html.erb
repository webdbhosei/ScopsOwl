<%= form_for(chat_group) do |f| %>
  <% if chat_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat_group.errors.count, "error") %> prohibited this chat_group from being saved:</h2>

      <ul>
      <% chat_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
