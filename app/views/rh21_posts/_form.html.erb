<%= form_for(rh21_post) do |f| %>
  <% if rh21_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rh21_post.errors.count, "error") %> prohibited this rh21_post from being saved:</h2>

      <ul>
      <% rh21_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if params[:thread_id] %><input type="hidden" id="thread_id" name="post[thread_id]" value="<%= params[:thread_id] %>" /><% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, size: "60x3"%>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-lg btn-primary"%>
    <%= link_to 'Back', "/rh21_threads/" %>
  </div>
<% end %>
